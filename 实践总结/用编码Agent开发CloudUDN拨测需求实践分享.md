# 用编码Agent开发CloudUDN拨测需求实践分享

## 第一章：实践概览 —— 编码Agent驱动的CloudUDN拨测开发
<!-- 本章主要概述本次实践的背景与目标。介绍如何利用编码Agent技术从零开始构建CloudUDN拨测功能，简述项目规模、所用技术栈及最终达成的开发效率提升成果。 -->

## 第二章：需求透视 —— CloudUDN拨测系统的业务挑战
<!-- 本章详细介绍CloudUDN拨测的具体业务需求。包括拨测任务的调度、执行机管理、WebSocket通信协议的设计、数据采集与上报等核心功能点，以及由此带来的开发复杂度。 -->

## 第三章：实战回放 —— 编码Agent驱动“执行机双链接改造”全流程

### 3.1 辅助设计文档修改
<!--
- **输入**：
  1. 原始需求：CloudUDN拨测需实现控制流与数据流分离，建立双WebSocket链接以避免大数据阻塞控制指令。
  2. 现有资产：V4版本《执行机管理-WebSocket通信协议设计.md》。
- **人机协作过程**：
  1. **现状解读**：用户上传V4版协议文档，要求Agent深度解析现有方案；Agent梳理出当前单链路在传输大文件时可能阻塞心跳和控制指令的隐患。
  2. **方案研讨**：用户提出“双链接、数控分离”的核心需求；Agent分析修改点，建议拆分为Control-Channel和Data-Channel，并列出协议变更清单；双方经过多轮对话对齐了Session管理细节。
  3. **初次执行**：Agent基于讨论结果修订设计文档；修改完成后，用户开启新对话窗口，指令Agent对新文档进行质量检查（Review），同时自己进行人工审查。
  4. **关键纠偏**：用户敏锐发现Agent的初版设计采用了“单物理连接复用逻辑通道”的方案，指出现实中仍会存在TCP队头阻塞问题，违背了“物理隔离”初衷；用户指令Agent将设计调整为两条独立的物理TCP连接。
  5. **迭代完善**：Agent重新修正时序图与连接建立流程，明确区分两个WebSocket Endpoint；用户与Agent再次进行双重检查，确认逻辑无误后定稿。
- **输出标准**：
  1. 逻辑自洽：彻底解决数据阻塞控制问题，双链路物理分离。
  2. 格式规范：Markdown语法正确，时序图清晰表达双链路交互。
- **最终输出**：修订完成的《执行机管理-WebSocket通信协议设计.md》，包含独立的控制与数据通道定义、双重鉴权及异常处理流程。

> **【演示素材预留】**
> *   **演示场景1：需求理解与方案分析**
>     *   *素材*：录屏或截图 —— 展示Agent如何快速总结V4版本协议痛点，并列出“数控分离”的详细修改点清单（体现Agent的理解力）。
> *   **演示场景2：关键纠偏对话（高光时刻）**
>     *   *素材*：对话记录截图 —— 重点展示用户指出“单物理连接”缺陷的Prompt，以及Agent承认错误并立即重新生成“双物理连接”方案的过程（体现人机协作与纠错）。
> *   **演示场景3：架构设计图演进**
>     *   *素材*：Mermaid时序图对比 —— 展示初版（逻辑通道复用）vs 终版（物理通道分离）的时序图差异，直观呈现设计优化结果。
>
> **【实践要点总结】**
> *   **输出标准前置**：在设计初期即明确“物理隔离”、“协议兼容”等硬性指标，有效避免了方向性错误。
> *   **交叉检查机制**：建立“Agent换角色自查（新对话窗口）”+“人工专家Review”的双重校验流程，确保了文档的严谨性。
> *   **架构决策主导**：Agent擅长补全细节，但核心架构决策（如解决TCP队头阻塞）仍需依靠人的经验进行关键纠偏。
-->

#### 1. 输入与背景
*   **原始需求**：CloudUDN拨测系统需要支持大文件（APK/脚本/日志/截图）的高效传输，同时必须保证控制指令（如立即停止任务）的实时性，不能出现因文件上传导致控制指令卡顿的情况。
*   **现有资产**：V4版本《执行机管理-WebSocket通信协议设计.md》。该版本采用了“单WSS连接 + 应用层优先级队列”的方案，试图通过软件逻辑层面的排队来解决阻塞问题。

#### 2. 人机协作过程
本次设计修改经历了从“逻辑分离”到“物理分离”的深度迭代，充分体现了Agent在细节补全上的优势与人类在核心架构把控上的不可替代性。

1.  **现状解读与痛点挖掘**
    *   **人**：上传V4协议文档，询问：“当前单链路方案在几十MB文件传输时，能否保证心跳不丢失？”
    *   **Agent**：深度解析后指出，虽然应用层设计了高低优先级队列，但在TCP传输层，一旦大文件的二进制流填满了TCP发送窗口，后续的控制信令（即使在应用层排在前面）仍需等待TCP缓冲区排空。这就是典型的**TCP队头阻塞（Head-of-Line Blocking）**问题，单链接无法彻底根治。

2.  **方案研讨（数控分离）**
    *   **人**：提出核心设计方向：“我们需要彻底的物理隔离。设计双WebSocket链接，一条专门传控制信令（Control-Link），一条专门传大数据（Data-Link）。”
    *   **Agent**：迅速响应，列出了协议变更清单：建议将URL拆分为 `/ws/executor/control` 和 `/ws/executor/data`；Control Link 仅处理JSON文本，Data Link 仅处理Binary流。

3.  **关键纠偏（高光时刻）**
    *   **Agent（初版方案）**：在初步生成的草稿中，Agent为了“代码复用”，试图复用同一个 Endpoint 类，仅在内部逻辑上区分通道。
    *   **人（纠偏）**：敏锐指出这违背了“物理隔离”的初衷，现实中仍可能存在资源竞争。指令Agent：“不要混用。请设计两个独立的 Endpoint 类 (`ControlLinkEndpoint` 和 `DataLinkEndpoint`)，并分别配置不同的缓冲区大小（Control用小缓存快响应，Data用大缓存高吞吐）。”
    *   **Agent（修正）**：承认错误并立即修正了类图和配置代码，确认了在 `WebSocketJsr356Config` 中注册两个独立 `ServerEndpoint` 的最终架构。

4.  **迭代完善（鉴权流程）**
    *   **人**：询问“双链接如何保证是同一个Session？”
    *   **Agent**：补充了详细的时序图，定义了“主从绑定”流程：
        1.  Control Link 先建立，通过 CHAP 挑战响应完成认证，生成 Session Token。
        2.  Data Link 后建立，首条消息发送 `{type: "auth", token: "..."}` 完成绑定。

#### 3. 最终输出
修订完成的 **V5版《执行机管理-WebSocket通信协议设计.md》**。新设计明确了双端点架构：
*   **Control Endpoint** (`/ws/executor/control`)：配置 2MB 文本缓冲区，保障信令实时性。
*   **Data Endpoint** (`/ws/executor/data`)：配置 10MB 二进制缓冲区，保障大文件吞吐。
这一设计随后被后端代码完整实现，从架构层面彻底消除了数据流阻塞控制流的风险。

#### 4. 实践要点总结
*   **输出标准**：高质量软件实现设计文档。文档应达到**“编码就绪”**深度，具体包含：**静态架构**（精确到类图与方法签名的包结构设计）、**动态流程**（覆盖异常分支的完整时序图）、**接口契约**（字段级的协议/API定义）及**关键决策**（如缓冲区大小、线程模型），确保开发人员能依据文档直接编写代码而无须二次确认。
*   **交叉检查机制**：建立“Agent换角色自查（新对话窗口）”+“人工专家Review”的双重校验流程，确保了文档的严谨性。
*   **人员能力进阶**：要求开发者具备**架构视角的判别力**。Agent 擅长补全细节，但容易生成“逻辑自洽但工程不可行”的方案。只有开发者具备识别系统性风险（如解决 TCP 队头阻塞）的能力，才能在关键节点进行有效纠偏，这是产出高质量设计的核心门槛。
*   **高效配合模式**：遵循**“人定骨架，Agent 填肉”**的原则。开发者不再是文档的撰写者，而是**设计约束的定义者**。高效配合的关键在于：人负责输入核心设计原则（如物理隔离、鉴权逻辑）和验收标准，Agent 负责将这些原则实例化为图表、接口和文字，人机互补达成“1+1>2”。
    > *例如在本案例的文档设计中，**开发者**仅定义了“必须采用双物理链路防止队头阻塞”这一核心骨架；而**Agent**则承担了繁重的“填肉”工作——自动补全了双链路的鉴权交互时序图、设计了具体的 `ControlLinkEndpoint` 与 `DataLinkEndpoint` 类结构、并生成了完整的 JSON 信令定义。这种分工让开发者从枯燥的排版和绘图中解放出来，专注于系统的高可用性设计。*

### 3.2 核心代码生成
<!--
- **输入**：
  1. 设计文档：修订后的《执行机管理-WebSocket通信协议设计.md》（协议细节已完全确定）。
  2. 引导信息：项目根目录结构、模块划分（Controller/Service/Entity）、现有依赖版本（Spring Boot 2.7.x）。
- **人机协作过程**：
  1. **上下文注入**：鉴于Agent对大型项目结构的感知局限，用户主动提供项目根目录路径，指引Agent定位至`dialingtest-service`模块。
  2. **批量生成**：Prompt指令“基于设计文档，实现WebSocket双通道Handler及SessionManager”，Agent一次性生成了服务端处理逻辑、心跳机制及前端React Hook。
  3. **编译/启动排错**：
      *   用户尝试启动应用，遇到Bean循环依赖与Lombok注解失效问题。
      *   用户直接粘贴报错日志到对话框。
      *   Agent分析日志，快速给出“拆分Configuration类”与“修正pom.xml依赖scope”的修复补丁。
  4. **实现一致性验收**：
      *   代码生成量较大，人工逐行Review效率低。用户采取策略：**宏观人工审查 + 微观Agent验收**。
      *   **人工审查**：重点检查新引入的第三方库（如WebSocket依赖）版本是否冲突、项目层级结构是否乱套。
      *   **Agent验收**：开启新对话窗口，上传新生成的代码文件与原始设计文档，Prompt要求“检查代码实现是否完全覆盖了文档中的所有协议字段与流程”。
      *   Agent反馈：指出代码中漏掉了“断连时的资源清理逻辑”，用户随即指令补全。
- **输出标准**：
  1. 零编译错误：代码无语法问题，服务正常启动。
  2. 协议一致性：代码逻辑严格对应设计文档中的每一个信令交互。
- **最终输出**：
  1. 后端：稳定运行的WebSocket服务端（Handler, Service）。
  2. 前端：可用的React连接状态组件。

> **【演示素材预留】**
> *   **演示场景1：一次性生成的震撼**
>     *   *素材*：代码Diff截图 —— 展示Agent一次性生成数百行核心业务代码的壮观场面（体现文档完备带来的代码生成高效率）。
> *   **演示场景2：实现一致性检查**
>     *   *素材*：验收对话截图 —— 展示Agent指出“代码漏掉资源清理逻辑”的对话片段，证明Agent进行“自我监察”的可行性。
>
> **【实践要点总结】**
> *   **文档驱动开发（DDD）**：3.1节高质量的设计文档是本节高效生成的基石，直接降低了Prompt的复杂度。
> *   **分层审查策略**：面对大量生成代码，人工聚焦“工程结构与依赖”，Agent聚焦“逻辑细节与协议覆盖”，实现效率最大化。
> *   **引导式上下文**：主动提供项目路径与模块结构，有效弥补了当前工具在大型项目索引上的短板。
-->

### 3.3 测试设计生成
<!--
- **输入**：
  1. 业务代码：生成的双链接核心逻辑。
  2. 场景描述：断网重连、并发竞争、消息幂等性等关键点。
- **人机协作过程**：
  1. **人**：提供核心Service代码，询问“针对双链接切换逻辑，有哪些边界场景需要测试？”
  2. **Agent**：分析代码分支，列出“主备同时断开”、“网络抖动导致频繁切换”、“消息乱序”等5个高危场景。
  3. **人**：选中高危场景，指令“生成详细的测试用例，包含前置条件和预期步骤”。
  4. **Agent**：输出结构化的测试用例表格。
  5. **人**：补充“鉴权失效”等安全相关场景，完善用例库。
- **输出标准**：
  1. 覆盖率：覆盖所有核心业务场景。
  2. 清晰度：测试步骤可执行，预期结果明确。
- **最终输出**：结构化的测试用例列表（包含用例ID、前置条件、步骤、预期结果）。
-->

### 3.4 集成测试代码实现
<!--
- **输入**：
  1. 测试用例：上一步生成的测试设计。
  2. 待测代码：后端核心业务类。
  3. 测试框架规范：JUnit 5 + Mockito。
- **人机协作过程**：
  1. **人**：Prompt要求“编写集成测试，需Mock一个WebSocket Client模拟执行机行为”。
  2. **Agent**：编写MockServer类，模拟发送心跳和注册报文。
  3. **人**：运行测试发现Mock行为过于理想化，指示“模拟网络延迟和随机断连”。
  4. **Agent**：修改Mock逻辑，引入随机延迟与丢包模拟。
  5. **人**：指令“实现测试脚本，验证主备自动切换功能”。
  6. **Agent**：编写JUnit测试方法，断言切换前后的Session状态变化。
- **输出标准**：
  1. 可执行性：测试代码直接运行通过。
  2. 真实性：Mock行为尽可能逼近真实执行机。
- **最终输出**：包含Mock逻辑的集成测试类文件（如`DualLinkIntegrationTest.java`），能够自动化验证核心流程。
-->

## 第四章：模式转变 —— Agent模式 vs 传统开发的深度对比
<!-- 本章从开发效率、代码质量、心智负担等维度，对比编码Agent辅助开发模式与传统人工开发模式的差异。总结在Prompt工程、上下文管理等方面的实践经验与避坑指南。 -->

## 第五章：能力演进 —— 编码Agent与Cursor的能力差异分析
<!-- 本章客观分析本次使用的编码Agent与Cursor在代码理解深度、多文件协同、工程化感知等方面的能力差距，探讨不同工具在辅助编程领域的优势区间与改进方向。 -->
