import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const UeStatus = () => {
  return (
    <div>
      <Title level={2}>UE状态</Title>
      <p>这里是UE状态页面。</p>
    </div>
  );
};

export default UeStatus;

