import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const TaskRecords = () => {
  return (
    <div>
      <Title level={2}>任务记录</Title>
      <p>这里是任务记录页面。</p>
    </div>
  );
};

export default TaskRecords;

